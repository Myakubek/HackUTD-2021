#Main class to run everything
>>>class Review:
  def __init__(post, review, reviewerName, date, sentimentVal, memberLength, policy):
    post.review = review
    post.reviewerName = reviewerName
    post.date = date
    post.sentimentVal = sentimentVal
    post.memberLength = memberLength
    post.policy = policy
#to be done:
#execute sort function
#execute sentiment function
